**Knight Travails**
---

This project implements a solution to find the shortest path for a knight on a chessboard from a starting position to a target position. It uses a queue-based breadth-first search (BFS) algorithm to achieve this.
